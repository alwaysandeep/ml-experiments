# Core ML packages
# Note: Install PyTorch first with CPU-only builds:
# uv pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cpu

# Then install these packages:
fastai==2.8.6
ipykernel==7.2.0
ipywidgets==8.1.8
numpy==2.3.5
matplotlib==3.10.8
pandas==3.0.0
seaborn==0.13.2
